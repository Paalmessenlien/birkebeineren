# Playwright MCP artifacts
.playwright-mcp/

# screenshots / working images (not used by the app)
*.png

# source photos used to digitise the felt course (not needed by the app)
*.jpeg
*.jpg

# local Claude Code harness files
.claude/
